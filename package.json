{
  "name": "apy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf api/backend/venv api/backend/__pycache__ && cd frontend && npm install && npm run build && cp -r dist ../dist"
  }
}
