{
  "name": "apy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp -r backend api/backend 2>/dev/null || true && cd frontend && npm install && npm run build && cp -r dist ../dist"
  }
}
